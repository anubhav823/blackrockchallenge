# Blackrock Spring Boot

Simple Spring Boot starter project scaffolded by Copilot.

Build:

```bash
mvn package
```

Run:

```bash
mvn spring-boot:run
# or
java -jar target/blackrock-0.0.1-SNAPSHOT.jar
```
